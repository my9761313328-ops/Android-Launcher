<?xml version="1.0" encoding="utf-8"?>
<com.facebook.shimmer.ShimmerFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Media placeholder. Real media sizes itself to the ad's own
             aspect ratio once loaded, so this is just a sensible default
             height for the loading state — no gap either way since both
             this and the real media container start flush with no
             padding. -->
        <View
            android:layout_width="match_parent"
            android:layout_height="170dp"
            android:background="@drawable/bg_shimmer_bar" />

        <!-- Icon + headline/advertiser column, mirroring the real ad row. -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="top"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="@dimen/sponsored_media_vertical_gap">

            <View
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_marginEnd="10dp"
                android:background="@drawable/bg_shimmer_button" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <View
                    android:layout_width="match_parent"
                    android:layout_height="16dp"
                    android:background="@drawable/bg_shimmer_bar" />

                <View
                    android:layout_width="90dp"
                    android:layout_height="12dp"
                    android:layout_marginTop="6dp"
                    android:background="@drawable/bg_shimmer_bar" />

                <!-- Body placeholder, two lines like the real ad's body
                     text so the loaded card doesn't jump in height. -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="12dp"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/bg_shimmer_bar" />

                <View
                    android:layout_width="200dp"
                    android:layout_height="12dp"
                    android:layout_marginTop="6dp"
                    android:background="@drawable/bg_shimmer_bar" />

            </LinearLayout>

        </LinearLayout>

        <!-- Full-width CTA placeholder, matching the real button's size
             and position instead of a small side-by-side box. -->
        <View
            android:layout_width="match_parent"
            android:layout_height="46dp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/bg_shimmer_button" />

    </LinearLayout>

</com.facebook.shimmer.ShimmerFrameLayout>
